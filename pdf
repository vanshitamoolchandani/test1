import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ProtocolscraperComponent } from './protocolscraper.component';

describe('ProtocolscraperComponent', () => {
  let component: ProtocolscraperComponent;
  let fixture: ComponentFixture<ProtocolscraperComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ProtocolscraperComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ProtocolscraperComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
